# Hasura CLI project file used by Nhost's migrations-and-metadata deployment step.
# Nhost supplies the real endpoint/admin credentials during deployment.
version: 3
endpoint: http://localhost:8080
admin_secret: adminsecretkey
metadata_directory: nhost/metadata
migrations_directory: nhost/migrations
seeds_directory: nhost/seeds
